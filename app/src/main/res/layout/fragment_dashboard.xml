<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusable="true"

    tools:context=".ui.DashboardFragment">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:layout_height="wrap_content">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:id="@+id/upComingLabel"
        android:textAlignment="center"
        android:text="@string/up_coming"
        android:textSize="20sp"
        android:layout_weight="1"
        android:layout_alignParentTop="true"
        />
  <androidx.recyclerview.widget.RecyclerView
      android:layout_width="match_parent"
      android:layout_height="0dp"
      android:layout_below="@id/upComingLabel"
      android:layout_weight="7"
      android:layout_marginTop="10dp"
      android:id="@+id/upcomingRecycler"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:id="@+id/snoozedLabel"
        android:textAlignment="center"
        android:text="@string/snoozed_amp_skipped"
        android:layout_margin="10dp"
        android:textSize="20sp"
        android:layout_weight="1"
        android:layout_alignParentTop="true"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/upComingLabel"
        android:layout_weight="7"
        android:id="@+id/snoozedRecycler"/>



</androidx.appcompat.widget.LinearLayoutCompat>

</ScrollView>